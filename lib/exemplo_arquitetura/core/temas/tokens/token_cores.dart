import 'package:flutter/material.dart';

class CorTokens {
  // Seeds por marca
  static const padrao = Color(0xFF0057FF);
  static const govBr = Color(0xFF2E7D32);
  static const suap = Color(0xFFF57C00);

  // Neutros
  static const branco = Colors.white;
  static const preto = Colors.black;
}